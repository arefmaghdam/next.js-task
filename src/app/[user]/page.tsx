"use client";

export default async function Users() {

    
//   const userData = await getUserAPI(id);
//   console.log(userData);

  return (
    <main className="max-h-screen items-center justify-between p-5">
      <div className="mb-8 text-2xl font-bold">Users</div>
      <div>
        {/* {userData.map((item: Users) => (
          
        ))} */}
      </div>
    </main>
  );
}
